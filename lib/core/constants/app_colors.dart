import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primary = Color.fromRGBO(76, 175, 80, 1); // Green
  static const primaryLight = Color.fromRGBO(129, 199, 132, 1); // Light Green
  static const primaryDark = Color.fromRGBO(56, 142, 60, 1); // Dark Green

  // Secondary Colors
  static const secondary = Color.fromRGBO(0, 150, 136, 1); // Teal
  static const secondaryLight = Color.fromRGBO(77, 208, 225, 1); // Light Teal
  static const secondaryDark = Color.fromRGBO(0, 121, 107, 1); // Dark Teal

  // Background Colors
  static const background = Color.fromRGBO(248, 250, 252, 1); // Light Gray Blue
  static const surface = Color.fromRGBO(255, 255, 255, 1); // White
  static const darkSurface = Color.fromRGBO(
    30,
    30,
    30,
    1,
  ); // For dark theme surface
  static const darkBackground = Color.fromRGBO(
    18,
    18,
    18,
    1,
  ); // Dark Background

  // Text Colors
  static const textPrimary = Color.fromRGBO(33, 33, 33, 1); // Almost Black
  static const textSecondary = Color.fromRGBO(117, 117, 117, 1); // Mid Grey
  static const textLight = Color.fromRGBO(158, 158, 158, 1); // Light Grey
  static const white = Color.fromRGBO(255, 255, 255, 1);

  // Status Colors
  static const error = Color.fromRGBO(244, 67, 54, 1); // Red
  static const warning = Color.fromRGBO(255, 193, 7, 1); // Amber
  static const success = Color.fromRGBO(76, 175, 80, 1); // Green
  static const info = Color.fromRGBO(33, 150, 243, 1); // Blue

  // Category Colors
  static const general = Color.fromRGBO(156, 39, 176, 1); // Purple
  static const technology = Color.fromRGBO(33, 150, 243, 1); // Blue
  static const sports = Color.fromRGBO(255, 87, 34, 1); // Orange
  static const entertainment = Color.fromRGBO(233, 30, 99, 1); // Pink
  static const business = Color.fromRGBO(0, 150, 136, 1); // Teal
  static const health = Color.fromRGBO(76, 175, 80, 1); // Green
  static const science = Color.fromRGBO(121, 85, 72, 1); // Brown

  // Gradient Colors
  static const gradientStart = Color.fromRGBO(76, 175, 80, 1);
  static const gradientEnd = Color.fromRGBO(0, 150, 136, 1);

  // Card Colors
  static const cardLight = Color.fromRGBO(255, 255, 255, 1);
  static const cardDark = Color.fromRGBO(45, 45, 45, 1);
  static const cardShadow = Color.fromRGBO(0, 0, 0, 0.1);
}
